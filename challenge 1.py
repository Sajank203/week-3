

# Prompt the user to input the radius of the circle
K= float(input("input the first Vertex of the Triangle : "))
j=float(input("input the second vertex of the triangle : "))

# calculate the area of the circle using the formula: area = π * r^2
area = K * j

# Display the result, including the radius and calculated area 
print("The area of the triangle of size " + str(K) +" & " + str(j) + " is: " + str(area))